{
  "name": "diffhero",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A free, private, client-side diff checker for text and code.",
  "scripts": {
    "build": "node engine/build.mjs",
    "dev": "node engine/build.mjs && node scripts/dev-server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
